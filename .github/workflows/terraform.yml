name: DevOps Pipeline

on:
  push:
    branches:
      - feature/aks

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test
        run: echo "Pipeline is running successfully"

      - name: Build
        run: echo "Build completed successfully"